{% if newFile %}
services:
{% endif %}

    # Service for the {{ name }} twig extension
    {{ bundle_alias }}.twig.{{ name }}_extension:
        class:  {{ fqcn }}
        tags:
            - { name: twig.extension }
